> **SUPERSEDED (2026-06-10)** — see `docs/HANDOFF_TO_WIN.md` (authoritative). Kept for history; contains pre-RL-verdict plans.

# Status 2026-06-07 — Sim-7 debrief & final-week pipeline

**Deadline: final tournament 2026-06-14 23:55. Sim-7 result: rank 14/69 (distill100b).**

## Sim-7 audit (512 games, [moyu]caiest)

- mean **+5.43/game**; ranks: 41% 1st · 38% 2nd · 16% 4th; wins 27% (fan 8–33); draws 5.5%
- **deal-in 18%** — the "<2%" figure from self-play was monoculture blindness; defense is a real lever
- per-turn verdicts all OK (0 RE / 0 TLE / 0 WA from us across 44,905 responses); opponents: 60 RE, 3 TLE, 7 WA
- memory peaks AT 512MB (865 turns in the 448–512 band) — zero headroom but never died
- **5 wrong-HUs (WH), −30 each — OUR BUG, now fixed** (below)

## The WH bug (fixed in `3f1081e`)

All 5 WH games shared one signature: our `CHI` response **preempted by another player's PENG**.
`_replay_event` (the JSON full-history path Botzone actually exercises) applied our recorded claim
unconditionally → permanently desynced hand → phantom HU → judge `ERROR_NOT_WIN` → −30.
Fix: a recorded discard-claim is applied only when the judge's next-request echo confirms it.
Verified by replaying the real Botzone histories: all 5 WH decision points now return PASS.
(The live keep-running path already had UnChi/UnPeng rollback — only replay was wrong.)
Old-code control test running to confirm residual clean-game mismatches are torch-1.4-vs-2.5
near-tie noise, not behavior change.

## Data-loss recovery (box 38702735, /root/mahjong)

- Official 5.87M-decision dataset rebuilt: `cooked_{obs,mask,act}.npy` N=5,865,816 ✓ (exact match)
- Teacher data: sim-6 chunjiandu 5,272 + sim-7 xiaoxunge 6,865 = **12,137 decisions** (`topwinners_all.npz`)
- Official judge recompiled (`-D_BOTZONE_ONLINE` required!) + full h2h harness restored

## Candidates & evals (h2h = 2v2 seat-rotated official-judge games vs distill100b)

| Candidate | Recipe | Held-out agree (base→final) | h2h vs distill100b |
|---|---|---|---|
| sim6cand (s2800) | sim6 5.3k, frac .3, lr 5e-5 | 0.615→0.672 | **LOST 41–55, net −388/100g → rejected** |
| V1 (s600) | same, early stop | →0.667 | running |
| V2 (lr 2e-5, s1400) | frac .2 | →0.654 | queued |
| **V3A (s800, combined 12k)** | frac .3, lr 5e-5 | **0.669→0.722** | queued |
| V3B (lr 2e-5, s2000, combined) | frac .2 | training | queued |
| SAFE_DISCARD A/B | deploy flag, no retrain | n/a | queued last |

Lesson re-learned: champ agreement is a proxy; the s2800 model raised agreement most and LOST.
Ship rule: **a candidate ships only if it does NOT lose its h2h** (and ladder A/B remains the final word).

## Upload checklist (when you're back)

1. **Re-upload the bot CODE zip — REQUIRED regardless of model choice** (WH fix):
   `deploy/caiest_cnn_bot.zip` (md5 `064a49cbbf67674bfaacb8c3061e1a09`, includes safe_discard.py, SAFE off by default)
2. Storage `data/cnn.pkl`: keep **distill100b** (md5 `7e45c41309502865b824f90b41a0a537`) unless a
   variant wins its h2h — then A/B the winner as a SECOND bot on the ladder first.
3. Optional 2nd-bot A/Bs, in priority order: (a) best h2h-non-losing variant, (b) distill100b + `SAFE_DISCARD=1`.

All checkpoints live on box 38702735 `/root/mahjong/ckpt/`; sync any winner back to the repo before
the box disappears (it's a flaky free box!).
