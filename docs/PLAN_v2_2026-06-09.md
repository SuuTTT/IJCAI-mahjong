> **SUPERSEDED (2026-06-10)** — see `docs/HANDOFF_TO_WIN.md` (authoritative). Kept for history; contains pre-RL-verdict plans.

# Plan v2 (2026-06-09) — ride the top-30 ladder data to the deadline (06-14 23:55)

WINNER (pending fresh-wall confirm): lad_chunjiandu +5526 vs distill100b +4996 (+530) on the STRONG
gauntlet (unconfounded). RECIPE THAT WORKS: coherent #1 teacher (chunjiandu) x 12x augmentation x real
top-30 ladder data. Diverse mix (all-top30) ties (-54); fresh teacher (raven) loses (-293) -> SHARPEN
the #1 lineage, don't switch teachers.

P1 CONFIRM (gate): lad_chunjiandu vs distill100b on FRESH walls (400000). Scale only if it holds.
P2 RECIPE TUNE (current data): champ-frac {0.3,0.5} x steps {600,1000}; + chunjiandu-ladder UNION
   sim6-chunjiandu (more coherent #1 data). Gauntlet-gate (strong gate), keep best.
P3 SCALE (rolling to 10GB): at each +~250 chunjiandu-games milestone, re-extract -> re-distill best
   recipe (12x aug) -> strong gauntlet -> keep best checkpoint. Data firehose -> climbing candidate.
P4 LADDER A/B (Botzone up): best vs distill100b on the real ladder = true test (gauntlet is a proxy).
P5 LOCK (06-13/14): best CONFIRMED model + WH-fixed zip (numpy-primary 91MB option). lock-check cron 06-13.

Guardrails: read numbers from logs; >=200 games on disjoint walls before claiming; coherence>diversity;
agreement != play (gauntlet-gate everything); the ladder overrides the gauntlet.
