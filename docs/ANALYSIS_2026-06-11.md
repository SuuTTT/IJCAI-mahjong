# IJCAI Mahjong — final pre-tournament analysis (2026-06-11)

## SHIP (frozen, verified)
**`bot_lad_chunjiandu.zip`** (`ad016476…`) + Storage `data/cnn_lad_chunjiandu.pkl` (`d517e6a9…`).
Fallback: `bot_distill100b.zip` (`db86fd5b…`) + `cnn_distill100b.pkl` (`7e45c413…`).
`md5sum -c deploy/ship/MD5SUMS.txt` = all OK. The zips are pre-experiment builds — none of this
session's opt-in search/defense code is inside. Upload deadline buffer: **06-13 evening** (contest
closes 06-14 23:55).

## Pre-registered swap rule (written BEFORE the ladder data arrives)
The user is launching real ladder tables (ours + 3 top-30 bots; collector auto-captures; read with
`python3 tools/ladder_report.py`). Swap the ship to `distill100b` **only if** it leads
`lad_chunjiandu` by **≥ 0.5 net/game with ≥ 50 games on EACH bot**. Anything less = noise → keep
`lad_chunjiandu` (it won the in-house decisive bench 5/6 matchups, +181/144g). No post-hoc criteria.

## Ground truth that rewrote the model of the game (tools/ladder_report.py, 44 real games)
| metric | in-house self-play | real ladder |
|---|---|---|
| draws | 85–89% | **9%** |
| deal-ins | 1.8% | **16%** |
| wins | — | 20% (net −0.64/g) |

Both load-bearing beliefs — "conversion/draws is the disease" and "defense is a non-lever"
(ANALYSIS_2026-06-01) — were **self-play artifacts**: our passive clone field neither converts nor
punishes; the real field does both. Rule going forward: any "X is/isn't a lever" claim must state
which FIELD it was measured against. (Also: an earlier 39% deal-in figure was a parsing bug —
self-draw payments miscounted as deal-ins; 16% is the replay-verified number.)

## This session's experiments — all honest negatives (now 12 total nulls)
| lever | result | note |
|---|---|---|
| Champion clone (SeaMan, 2025 #1, 176k decisions, val_acc 0.81) | TIED ship | agreement ≠ play, again |
| PIMC, opponent-aware, greedy rollout | −129/288g held-out | fixes plan.py's solitaire flaw; still null — rollout can't convert |
| PIMC, fan-aware rollout | converts WORSE (draws 82→85%) | killed pre-deploy by isolation test |
| safe_discard (genbutsu top-2 swap) | **−317/240g, lost 6/6** | costs offense vs clone field |
| dead-shape FOLD (+swap) | **−267/240g, lost 6/6** | fold itself ≈ free (+50 vs swap = noise) |
| net-rollout PIMC | NO-GO (gate called early) | ~15% odds not worth the last 2 days |

All experimental code is committed and opt-in (`CAIEST_PIMC=1`, `SAFE_DISCARD=1|2`) — production
paths are byte-identical with env unset. Engine pieces worth keeping for the journal work:
`determinize.py` (verified hidden-world sampler), `csm_rollout.py` (opponent-aware, 210 r/s),
`pimc_search.py` (anytime, hard time-guard — never TLEs, unlike ladder #2 `hhh` which TLEs live).

## Eval-infra lessons (hard-won this session)
1. ssh1 stopped being dedicated (user's time-series jobs) → a gauntlet silently starved. ssh8 had a
   stale deadlocking bench → every game "stuck", net=0, looked like a result. **Always verify
   played>0 / low stuck before reading a number; only compare head-to-head on one idle box.**
2. Remembered absolutes ("+4119") are config-bound; they don't transfer across GN/timeout/box.
3. Position-control + held-out walls remain mandatory (they killed a V-search false positive and
   the PIMC smoke "+112").

## Upload checklist (06-13)
1. `cd deploy/ship && md5sum -c MD5SUMS.txt` — must be all OK.
2. Botzone: bot source = `bot_lad_chunjiandu.zip`; Storage file `data/cnn_lad_chunjiandu.pkl`.
3. After upload: play one ladder game; first-turn debug line must print `[cnn_lad_chunjiandu.pkl
   md5=d517e6a9…]`; verdict OK; memory < 512MB; turn time < 6s.
4. Confirm the TOURNAMENT entry points at the new version (not just the ladder bot).

## Open for the journal work (post-deadline, see RESEARCH_ROADMAP.md)
- R1 JAX env (Phase 1 validated ~85×/GPU; Phases 2–4 = win/claims/fan) → self-play RL at scale —
  the only path past the imitation ceiling that hasn't been falsified.
- The 12-null record + the self-play-artifact finding is itself a paper-grade negative-results story
  (honest-benchmarking angle, cf. honest-rl-bench).
- Defense variants deserve a REAL-field A/B (in-house was structurally unable to validate them).
